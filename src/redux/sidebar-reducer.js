let initialState = {
    friendsData: [
        {id: 1, name: 'Vasay'},
        {id: 2, name: 'Alena'},
        {id: 3, name: 'Denis'}
    ]
};

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;